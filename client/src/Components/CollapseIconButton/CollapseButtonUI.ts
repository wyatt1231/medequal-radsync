import { Paper } from "@mui/material";
import styled from "styled-components";

export const CollapseIconButtonContent = styled(Paper)`
  padding: 1em 1.5em;
  display: grid;
  grid-gap: 0.5em;

  .btn {
  }
  /* .MuiButton-label {
    display: grid;
    grid-auto-flow: column;
    grid-gap: 0.5em;
    justify-content: start;
    justify-items: start;
    align-items: center;
    align-content: center;
  } */
`;
